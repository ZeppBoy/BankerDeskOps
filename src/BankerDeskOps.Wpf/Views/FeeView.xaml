<UserControl x:Class="BankerDeskOps.Wpf.Views.FeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Fee Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="#f9f9f9" Padding="10" BorderBrush="#dee2e6" BorderThickness="1">
            <StackPanel>
                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="Product ID:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                </Grid>

                <Grid Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="Fee Name:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="Amount:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                </Grid>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Save" Command="{Binding SaveFeeCommand}" Height="35" Padding="15,5" Background="#28a745" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Edit Selected" Command="{Binding EditFeeCommand}" Height="35" Padding="15,5" Background="#ffc107" Foreground="Black" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteFeeCommand}" Height="35" Padding="15,5" Background="#dc3545" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Cancel" Command="{Binding CancelEditCommand}" Height="35" Padding="15,5" Background="#6c757d" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Load All" Command="{Binding LoadFeesCommand}" Height="35" Padding="15,5" Background="#17a2b8" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="By Product" Command="{Binding LoadFeesByProductCommand}" Height="35" Padding="15,5" Background="#6f42c1" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Fees}" SelectedItem="{Binding SelectedFee}"
                  CanUserAddRows="False" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="150"/>
                <DataGridTextColumn Header="Product ID" Binding="{Binding ProductId}" Width="150"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="4" Margin="0,10,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#dc3545" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>
