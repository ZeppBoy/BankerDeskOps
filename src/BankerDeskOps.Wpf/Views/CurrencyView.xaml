<UserControl x:Class="BankerDeskOps.Wpf.Views.CurrencyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Currency Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="#f9f9f9" Padding="10">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Currency Code:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding Code}" Margin="0,0,10,0" Padding="5"/>

            <Label Grid.Column="2" Content="Currency Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="3" Text="{Binding Name}" Padding="5"/>
        </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Save Currency" Command="{Binding SaveCurrencyCommand}" Height="35" Padding="15,5" Background="#28a745" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Edit" Command="{Binding EditCurrencyCommand}" Height="35" Padding="15,5" Background="#ffc107" Foreground="Black" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteCurrencyCommand}" Height="35" Padding="15,5" Background="#dc3545" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Cancel" Command="{Binding CancelEditCommand}" Height="35" Padding="15,5" Background="#6c757d" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Refresh" Command="{Binding LoadCurrenciesCommand}" Height="35" Padding="15,5" Background="#17a2b8" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}" CanUserAddRows="False" AutoGenerateColumns="False" Margin="0,40,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="200"/>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="#dc3545" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
