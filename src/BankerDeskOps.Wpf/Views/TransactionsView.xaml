<UserControl x:Class="BankerDeskOps.Wpf.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BankerDeskOps.Wpf.ViewModels">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Transactions Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Transfer Form -->
        <GroupBox Grid.Row="1" Header="Transfer Funds" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Source Account -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Source Account:" Margin="5" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding SourceAccounts}"
                          DisplayMemberPath="AccountNumber"
                           SelectedValuePath="Id"
                           SelectedValue="{Binding SourceAccountId, Mode=TwoWay}" Margin="5"/>

                <!-- Destination Account -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Destination Account:" Margin="5" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding DestinationAccounts}"
                          DisplayMemberPath="AccountNumber"
                           SelectedValuePath="Id"
                           SelectedValue="{Binding DestinationAccountId, Mode=TwoWay}" Margin="5"/>

                <!-- Amount -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Amount:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TransferAmount, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <!-- Description -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Description:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Transfer Funds" Command="{Binding TransferFundsCommand}" Width="120" Margin="5"/>
            <Button Content="Refresh" Command="{Binding LoadTransactionsCommand}" Width="80" Margin="5"/>
            <Button Content="Clear Form" Command="{Binding ClearFormCommand}" Width="80" Margin="5"/>
        </StackPanel>

        <!-- Transactions DataGrid -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TransactionType}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="Reference ID" Binding="{Binding ReferenceId}" Width="*"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Error Message -->
         <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="#dc3545" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
