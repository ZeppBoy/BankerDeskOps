<UserControl x:Class="BankerDeskOps.Wpf.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="User Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Create / Edit Form -->
        <Border Grid.Row="1" Background="#f9f9f9" BorderBrush="#dee2e6" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0 -->
                <Label Grid.Row="0" Grid.Column="0" Content="Username:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                <Label Grid.Row="0" Grid.Column="2" Content="Email:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                <Label Grid.Row="0" Grid.Column="4" Content="Role:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="5" ItemsSource="{Binding RoleValues}"
                          SelectedItem="{Binding SelectedRole}" Margin="0,2,0,2" Padding="5"/>

                <!-- Row 1 -->
                <Label Grid.Row="1" Grid.Column="0" Content="First Name:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                <Label Grid.Row="1" Grid.Column="2" Content="Last Name:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                <Label Grid.Row="1" Grid.Column="4" Content="Password:" VerticalAlignment="Center"/>
                <PasswordBox Grid.Row="1" Grid.Column="5" x:Name="PasswordBox" Margin="0,2,0,2" Padding="5"
                             PasswordChanged="PasswordBox_PasswordChanged"/>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Create User" Command="{Binding CreateUserCommand}" Height="35" Padding="15,5" Background="#28a745" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Update Selected" Command="{Binding UpdateUserCommand}" Height="35" Padding="15,5" Background="#007acc" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Activate" Command="{Binding ActivateUserCommand}" Height="35" Padding="15,5" Background="#20c997" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Deactivate" Command="{Binding DeactivateUserCommand}" Height="35" Padding="15,5" Background="#fd7e14" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteUserCommand}" Height="35" Padding="15,5" Background="#dc3545" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Refresh" Command="{Binding LoadUsersCommand}" Height="35" Padding="15,5" Background="#17a2b8" Foreground="White"/>
        </StackPanel>

        <!-- Users DataGrid -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                  CanUserAddRows="False" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username"   Binding="{Binding Username}"    Width="100"/>
                <DataGridTextColumn Header="Full Name"  Binding="{Binding FullName}"    Width="150"/>
                <DataGridTextColumn Header="Email"      Binding="{Binding Email}"       Width="200"/>
                <DataGridTextColumn Header="Role"       Binding="{Binding Role}"        Width="90"/>
                <DataGridTextColumn Header="Status"     Binding="{Binding Status}"      Width="90"/>
                <DataGridTextColumn Header="Last Login" Binding="{Binding LastLoginAt, StringFormat=yyyy-MM-dd HH:mm}" Width="130"/>
                <DataGridTextColumn Header="Created"    Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}"   Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Error / Status Message -->
        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="#dc3545" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
