<UserControl x:Class="BankerDeskOps.Wpf.Views.BankClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Bank Clients" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Create / Edit Form -->
        <Border Grid.Row="1" Background="#f9f9f9" BorderBrush="#dee2e6" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="220">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 0 -->
                    <Label Grid.Row="0" Grid.Column="0" Content="First Name:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                    <Label Grid.Row="0" Grid.Column="2" Content="Last Name:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                    <Label Grid.Row="0" Grid.Column="4" Content="Email:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="5" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,2" Padding="5"/>

                    <!-- Row 1 -->
                    <Label Grid.Row="1" Grid.Column="0" Content="Phone:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                    <Label Grid.Row="1" Grid.Column="2" Content="Date of Birth:" VerticalAlignment="Center"/>
                    <DatePicker Grid.Row="1" Grid.Column="3" SelectedDate="{Binding DateOfBirth}" Margin="0,2,10,2"/>

                    <Label Grid.Row="1" Grid.Column="4" Content="National ID:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="5" Text="{Binding NationalId, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,2" Padding="5"/>

                    <!-- Row 2 -->
                    <Label Grid.Row="2" Grid.Column="0" Content="Street:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                    <Label Grid.Row="2" Grid.Column="2" Content="City:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>

                    <Label Grid.Row="2" Grid.Column="4" Content="Postal Code:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="5" Text="{Binding PostalCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,2" Padding="5"/>

                    <!-- Row 3 -->
                    <Label Grid.Row="3" Grid.Column="0" Content="Country:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,10,2" Padding="5"/>
                </Grid>
            </ScrollViewer>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Create Client" Command="{Binding CreateClientCommand}" Height="35" Padding="15,5" Background="#28a745" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Update Selected" Command="{Binding UpdateClientCommand}" Height="35" Padding="15,5" Background="#007acc" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Suspend" Command="{Binding SuspendClientCommand}" Height="35" Padding="15,5" Background="#fd7e14" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Activate" Command="{Binding ActivateClientCommand}" Height="35" Padding="15,5" Background="#20c997" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Delete" Command="{Binding DeleteClientCommand}" Height="35" Padding="15,5" Background="#dc3545" Foreground="White" Margin="0,0,5,0"/>
            <Button Content="Refresh" Command="{Binding LoadClientsCommand}" Height="35" Padding="15,5" Background="#17a2b8" Foreground="White"/>
        </StackPanel>

        <!-- Clients DataGrid -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}"
                  CanUserAddRows="False" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name"  Binding="{Binding FirstName}"  Width="100"/>
                <DataGridTextColumn Header="Last Name"   Binding="{Binding LastName}"   Width="100"/>
                <DataGridTextColumn Header="Email"       Binding="{Binding Email}"       Width="200"/>
                <DataGridTextColumn Header="Phone"       Binding="{Binding PhoneNumber}" Width="110"/>
                <DataGridTextColumn Header="National ID" Binding="{Binding NationalId}"  Width="120"/>
                <DataGridTextColumn Header="City"        Binding="{Binding City}"        Width="100"/>
                <DataGridTextColumn Header="Country"     Binding="{Binding Country}"     Width="100"/>
                <DataGridTextColumn Header="Status"      Binding="{Binding Status}"      Width="90"/>
                <DataGridTextColumn Header="Created"     Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Error / Status Message -->
        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="#dc3545" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
